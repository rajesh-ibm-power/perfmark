language: java

arch:
  - amd64
  - ppc64le

jdk:
  - openjdk11

notifications:
  email: false

before_install:
 – chmod +x gradlew
 
before_script:
- chmod a+x gradlew

script: ./gradlew build

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock

cache:
  directories:
    - $HOME/.gradle/caches/modules-2/
    - $HOME/.gradle/wrapper/
